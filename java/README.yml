# Ansible playbook for installing Java


## Usage

- To install local machine
```bash
  ansible-playbook playbooks/openjdk.yml -i hosts.ini 
```

- To install in group of remote machines
```bash
  ansible-playbook playbooks/openjdk.yml -i hosts.ini --extra-vars "hosts=remote"
```
